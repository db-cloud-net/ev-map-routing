node_modules
.env
# Cursor IDE — ignore local editor state; keep plan stubs under .cursor/plans/ versioned
.cursor/*
!.cursor/plans/
.cursor/plans/*
!.cursor/plans/*.md
.gstack/
web/.next/
# Local Next.js overrides (secrets); use .env.example at repo root for docs
web/.env.local
# TypeScript incremental build cache
*.tsbuildinfo
api/dist/
# Local mirror refresh output (keep manifests/runbooks in docs; avoid large NDJSON in git)
api/mirror/snapshots/
# Runtime lock while a mirror refresh is in progress (do not commit)
api/mirror/**/refresh.lock
.vscode/
.tmp-screenshot2/
logs/
logs/*.ndjson
